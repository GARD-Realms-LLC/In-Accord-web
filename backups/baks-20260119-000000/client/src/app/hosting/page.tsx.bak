'use client';

import { useEffect, useMemo, useState } from 'react';

interface HostingAd {
        id: string;
        planName: string;
        shortDescription: string;
        pricePerMonth: string;
        features: string[];
        badge?: string;
        targetAudience?: string;
        ctaLabel: string;
        ctaUrl: string;
        contactEmail?: string;
        isFeatured?: boolean;
        createdAt: string;
        updatedAt: string;
        managedBy?: string;
      }

    const STORAGE_KEY = 'hosting_ads';

    const extractRoleTokens = (user: any): string[] => {
        const tokens = new Set<string>();
        const addToken = (value: unknown) => {
          if (typeof value === 'string') {
            const matches = value.match(/[A-Za-z0-9]+/g);
            if (matches) {
              matches.forEach((match) => tokens.add(match.toLowerCase()));
            }
          }
        };

        if (Array.isArray(user?.roles)) {
          user.roles.forEach(addToken);
        }

        addToken(user?.role);
















































































































































































































































































export default HostingPage;};  );    </div>            </section>              </div>                ))}                  </article>                    </footer>                      </div>                        {ad.managedBy && <span>· Managed by {ad.managedBy}</span>}                        <span>Updated {formatTimestamp(ad.updatedAt)}</span>                      <div className="flex items-center gap-3 text-xs text-gray-500 dark:text-gray-400">                      </a>                        {ad.ctaLabel}                      >                        className="inline-flex items-center justify-center rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white transition hover:bg-blue-700"                        rel="noopener noreferrer"                        target="_blank"                        href={ad.ctaUrl}                      <a                  <footer className="mt-6 flex flex-wrap items-center justify-between gap-3">                    </div>                      </ul>                        ))}                          </li>                            <span>{feature}</span>                            <span className="mt-1 h-1.5 w-1.5 rounded-full bg-blue-500" aria-hidden></span>                          <li key={index} className="flex items-start gap-2">                        {ad.features.map((feature, index) => (                      <ul className="space-y-2 text-sm text-gray-700 dark:text-gray-300">                      </div>                        )}                          <p className="text-xs uppercase tracking-wide text-gray-500 dark:text-gray-400">For {ad.targetAudience}</p>                        {ad.targetAudience && (                        <p className="text-3xl font-bold text-gray-900 dark:text-white">{ad.pricePerMonth}</p>                      <div className="rounded-2xl border border-gray-200 bg-white/70 p-4 dark:border-gray-700 dark:bg-gray-800/60">                      </p>                        {ad.shortDescription || 'Add a description to help buyers self-select the right plan.'}                      <p className="text-sm text-gray-600 dark:text-gray-300">                      </div>                        )}                          </span>                            {ad.badge}                          <span className="rounded-full bg-blue-100 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-blue-700 dark:bg-blue-900/40 dark:text-blue-200">                        {ad.badge && (                        <h3 className="text-xl font-semibold text-gray-900 dark:text-white">{ad.planName}</h3>                      <div className="flex items-center justify-between">                  <div className="space-y-4">                  >                    }`}                        : 'border-gray-200 bg-white dark:border-gray-700 dark:bg-gray-900'                        ? 'border-blue-400 bg-gradient-to-b from-blue-50 to-white dark:from-blue-900/40 dark:to-gray-900'                      ad.isFeatured                    className={`relative flex h-full flex-col justify-between rounded-2xl border p-6 shadow-sm transition hover:shadow-lg dark:border-gray-700 dark:bg-gray-900 ${                    key={ad.id}                <article                {sortedAds.map((ad) => (              <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">              </header>                </div>                  Last update {ads[0] ? formatTimestamp(ads[0].updatedAt) : '—'}                <div className="rounded-full border border-emerald-200 bg-emerald-50 px-4 py-1 text-xs font-medium text-emerald-700 dark:border-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-300">                </div>                  </p>                    Cards below are exactly what prospects see across the marketing funnel. Featured placements surface first.                  <p className="text-sm text-gray-600 dark:text-gray-300">                  <h2 className="text-2xl font-semibold text-gray-900 dark:text-white">Live hosting offers</h2>                <div>              <header className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between">            <section className="space-y-6">            )}              </section>                offers below or contact the hosting team for changes.                You have viewer access. Only users with either an Admin or Hosting role can publish new advertisements. Browse the              <section className="rounded-2xl border border-blue-200 bg-blue-50 px-6 py-5 text-sm text-blue-800 dark:border-blue-800 dark:bg-blue-900/40 dark:text-blue-200">          {!canManage && (            </div>              </div>                </div>                  </div>                    <div className="text-sm text-blue-100">Average advertised rate</div>                    <div className="text-3xl font-bold">{stats.avgPrice}</div>                  <div className="rounded-2xl border border-white/15 bg-white/10 p-5 backdrop-blur">                  </div>                    <div className="text-sm text-blue-100">Featured placements</div>                    <div className="text-3xl font-bold">{stats.featured}</div>                  <div className="rounded-2xl border border-white/15 bg-white/10 p-5 backdrop-blur">                  </div>                    <div className="text-sm text-blue-100">Active advertisements</div>                    <div className="text-3xl font-bold">{stats.total}</div>                  <div className="rounded-2xl border border-white/15 bg-white/10 p-5 backdrop-blur">                <div className="grid gap-4 sm:grid-cols-3">                </div>                  </p>                    performance, and keep your brand front-and-center with premium visual blocks.                    Craft hosting packages that speak to builders, teams, and enterprises. Launch new offers in moments, track                  <p className="text-lg text-blue-100">                  </h1>                    Deploy faster, scale smarter, and keep every byte blazing.                  <h1 className="text-4xl font-extrabold tracking-tight sm:text-5xl">                  </span>                    Lightning-fast cloud hosting                  <span className="inline-flex items-center gap-2 rounded-full border border-white/20 bg-white/10 px-4 py-1 text-sm font-medium backdrop-blur-sm">                <div className="max-w-3xl space-y-5">              <div className="relative flex flex-col gap-8 p-10 lg:p-16">              ></div>                aria-hidden                className="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(255,255,255,0.2),_transparent_60%)]"              <div            <div className="relative overflow-hidden rounded-3xl bg-gradient-to-r from-indigo-600 via-blue-600 to-cyan-500 text-white">    <div className="space-y-10 p-8">  return (        }, [ads]);          return { total, featured, avgPrice };          })();            return `$${Math.round(sum / numericPrices.length)}/mo avg`;            const sum = numericPrices.reduce((acc, price) => acc + price, 0);            if (!numericPrices.length) return 'Custom pricing';              .filter((price) => !Number.isNaN(price));              .map((ad) => Number(ad.pricePerMonth.replace(/[^0-9.]/g, '')))            const numericPrices = ads          const avgPrice = (() => {          const featured = ads.filter((ad) => ad.isFeatured).length;          const total = ads.length;  const stats = useMemo(() => {        };          );            })              return { ...ad, isFeatured: !ad.isFeatured };              if (ad.id !== id) return ad;            prev.map((ad) => {          setAds((prev) =>          if (!canManage) return;  const toggleFeatured = (id: string) => {        };          setAds((prev) => prev.filter((ad) => ad.id !== id));          if (!confirm('Remove this hosting advertisement?')) return;          if (!canManage) return;  const handleDelete = (id: string) => {        }, [ads]);          return [...ads].sort((a, b) => (b.isFeatured ? 1 : 0) - (a.isFeatured ? 1 : 0));  const sortedAds = useMemo(() => {        }, []);          }            setCanManage(false);          } catch {            setCanManage(hasHosting || hasAdmin);            const hasHosting = roleTokens.includes('hosting');            const hasAdmin = roleTokens.includes('admin');            const roleTokens = extractRoleTokens(parsed);            const parsed = JSON.parse(raw);            }              return;              setCanManage(false);            if (!raw) {            const raw = window.localStorage.getItem('currentUser');          try {          if (typeof window === 'undefined') return;  useEffect(() => {        }, [ads]);          }            // ignore persistence errors          } catch {            window.localStorage.setItem(STORAGE_KEY, JSON.stringify(ads));          try {          if (typeof window === 'undefined') return;  useEffect(() => {        const [canManage, setCanManage] = useState(false);        });          }            return DEFAULT_ADS;          } catch {            return parsed;            if (!Array.isArray(parsed) || parsed.length === 0) return DEFAULT_ADS;            const parsed = JSON.parse(stored) as HostingAd[];            if (!stored) return DEFAULT_ADS;            const stored = window.localStorage.getItem(STORAGE_KEY);          try {          if (typeof window === 'undefined') return DEFAULT_ADS;        const [ads, setAds] = useState<HostingAd[]>(() => {    const HostingPage = () => {      };        }          return iso;        } catch {          return new Date(iso).toLocaleString();        try {    const formatTimestamp = (iso: string) => {      ];        },          managedBy: 'System',          updatedAt: '2026-01-01T12:00:00.000Z',          createdAt: '2026-01-01T12:00:00.000Z',          isFeatured: false,          contactEmail: 'enterprise@example.com',          ctaUrl: 'https://example.com/enterprise-atlas',          ctaLabel: 'Schedule a strategy session',          targetAudience: 'Enterprises, FinTech, Healthcare, Gov',          badge: 'White-glove delivery',          ],            'Executive dashboards with budget forecasting',            'Quarterly resilience drills and performance tuning',            'SOC2 / HIPAA / GDPR compliance tooling included',            'Dedicated pod with private networking and WAF',          features: [          pricePerMonth: 'Let’s talk',          shortDescription: 'Dedicated clusters, compliance-ready, and run by an embedded SRE squad tailored to your org.',          planName: 'Enterprise Atlas',          id: 'ad-enterprise',        {        },          managedBy: 'System',          updatedAt: '2026-01-01T12:00:00.000Z',          createdAt: '2026-01-01T12:00:00.000Z',          isFeatured: true,          contactEmail: 'scale@example.com',          ctaUrl: 'https://example.com/hyperscale-pro',          ctaLabel: 'Book a migration consult',          targetAudience: 'Growing SaaS teams, agencies, enterprises',          badge: 'Most popular',          ],            '99.99% uptime SLA with runbook handoff',            'Advanced observability dashboards & alerting',            'Managed Postgres + Redis bundles included',            'Auto-scale clusters across 3 regions',          features: [          pricePerMonth: '$79/mo',          shortDescription: 'Built for scaling SaaS products that need autoscaling, observability, and bulletproof SLAs.',          planName: 'HyperScale Pro',          id: 'ad-scale',        {        },          managedBy: 'System',          updatedAt: '2026-01-01T12:00:00.000Z',          createdAt: '2026-01-01T12:00:00.000Z',          isFeatured: true,          contactEmail: 'hello@example.com',          ctaUrl: 'https://example.com/creator-cloud',          ctaLabel: 'Launch in under 5 minutes',          targetAudience: 'Solo founders, bloggers, makers',          badge: 'Starter favorite',          ],            '24/7 live chat with a real engineer in under 2 minutes',            'One-click app launcher (WordPress, Ghost, Next.js)',            'Global CDN acceleration and DDoS shielding',            '40 GB NVMe storage with daily snapshots',          features: [          pricePerMonth: '$19/mo',          shortDescription: 'Perfect for personal sites, portfolios, or early-stage projects that crave speed on a budget.',          planName: 'Creator Cloud',          id: 'ad-starter',        {    const DEFAULT_ADS: HostingAd[] = [      };n        return Array.from(tokens);